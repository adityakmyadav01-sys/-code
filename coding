import numpy as np
import matplotlib.pyplot as plt

# Data
categories = np.array(['A', 'B', 'C', 'D'])
values = np.array([15, 30, 45, 20])

# Plotting
plt.bar(categories, values, color='skyblue')
plt.title('Simple Bar Chart')
plt.show()
